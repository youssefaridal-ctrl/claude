# Local development infrastructure. App runs on the host (`npm run dev`).
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: selv
      POSTGRES_PASSWORD: selv
      POSTGRES_DB: selv
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U selv"]
      interval: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      retries: 10

volumes:
  pgdata:
